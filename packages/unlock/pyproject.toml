[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "unlock"
version = "0.1.0"
description = ''
readme = "README.md"
requires-python = ">=3.8"
keywords = []
dependencies = [
  "pexpect",
]

[project.scripts]
unlock = "unlock.cli:main"
