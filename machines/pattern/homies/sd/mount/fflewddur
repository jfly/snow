#!/usr/bin/env nix-shell
#!nix-shell -i bash -p sshfs

# Mount fflewddur's media at ~/media/fflewddur

# shellcheck shell=bash


set -euo pipefail

DIR=~/media/fflewddur/
mkdir -p "$DIR"
sshfs -o allow_root,reconnect fflewddur:/mnt/media "$DIR"
